function MuiStackSx() {
    const muiStackSx: {
        boxShadow: string,
        backdropFilter: string
    } = {
        boxShadow: "0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)",
        backdropFilter: "blur(4px)"
    };
    return muiStackSx;
}

export default MuiStackSx;